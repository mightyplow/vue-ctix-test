{
  "dependencies": {
    "vue": "^3.2.45"
  },
  "name": "vue-ctix",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {
    "ctix": "^1.6.0",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "build": "ctix single --noBackup -p ./tsconfig.json -o ./index.ts --noBackup -w -s",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
